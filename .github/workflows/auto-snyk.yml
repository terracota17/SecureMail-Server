name: Auto check PR and push
on:
  pull-request:
    branches:
      - master
      
jobs:
  check-auto-pr-with-snyk:
    runs-on: self-hosted
    steps:
      - name: Check out push
        uses: actions/checkout@v4
